---
export const prerender = true;
import Layout from '../layouts/Layout.astro';
import Sidebar from '../components/Sidebar.astro';
import TradeCard from '../components/TradeCard.astro';
import { getCollection } from 'astro:content';

// Fetch latest 3 trades
const latestTrades = (await getCollection('daily-trades')).sort(
	(a, b) => {
        const dateA = a.data.date ? new Date(a.data.date).valueOf() : 0;
        const dateB = b.data.date ? new Date(b.data.date).valueOf() : 0;
        return dateB - dateA;
    }
).slice(0, 3);

// Fetch latest 3 guides
const latestGuides = (await getCollection('fx-guides')).slice(0, 3);
---

<Layout title="トレ太のFX実践日記 - 負けないトレーダーを目指す日々の記録">
    <!-- Hero Section (Full Width) -->
    <div class="hero-wrapper">
        <div class="hero container">
            <div class="hero-content">
                <h1>FXで負け続ける男、<br />トレ太の逆襲記録</h1>
                <p>30代サラリーマンが「負けないFX」を目指して日々奮闘するリアルなドキュメンタリーブログ。</p>
            </div>
            <div class="hero-image">
                <!-- Placeholder -->
                <div class="char-placeholder">トレ太 & ブルベア</div>
            </div>
        </div>
    </div>

    <!-- Main Content Area (2 Columns) -->
    <div class="main-container container">
        <div class="content-area">
            <!-- Latest Articles Section -->
            <section class="latest-section">
                <h2 class="section-title">最新の実践記録</h2>
                
                {latestTrades.length > 0 ? (
                    <div class="article-grid">
                        {latestTrades.map(post => (
                            <TradeCard entry={post} />
                        ))}
                    </div>
                ) : (
                    <div class="empty-state">
                        <p>記事はまだありません。</p>
                        {import.meta.env.DEV && <p style="margin-top:10px;"><a href="/keystatic">管理画面で記事を作成</a></p>}
                    </div>
                )}
                
                <div class="more-link">
                    <a href="/practice" class="btn btn-outline">実践記録をもっと見る &rarr;</a>
                </div>
            </section>

             <!-- FX Guides Section -->
             <section class="latest-section mt-5">
                <h2 class="section-title">初心者向けFX解説</h2>
                <div class="article-list">
                    {latestGuides.length > 0 ? (
                        latestGuides.map(guide => (
                            <article class="list-card">
                                <a href={`/learn/${guide.slug}`} class="list-link">
                                    <div class="list-icon">🔰</div>
                                    <div class="list-content">
                                        <h3>{guide.data.title}</h3>
                                        <p>カテゴリー: {guide.data.category}</p>
                                    </div>
                                </a>
                            </article>
                        ))
                    ) : (
                        <p>解説記事はまだありません。</p>
                    )}
                </div>
            </section>
        </div>

        <!-- Sidebar -->
        <Sidebar />
    </div>
</Layout>

<style>
    /* Layout */
    .container {
        max-width: 1100px;
        margin: 0 auto;
        padding: 0 1rem;
    }
    .main-container {
        display: grid;
        grid-template-columns: 1fr 300px;
        gap: 2.5rem;
        margin-top: 3rem;
        margin-bottom: 4rem;
    }

    /* Hero */
    .hero-wrapper {
        background: linear-gradient(135deg, #e0f2fe 0%, #f0fdf4 100%);
        padding: 4rem 0;
        border-bottom: 1px solid #e5e7eb;
    }
    .hero {
        display: grid;
        grid-template-columns: 1.2fr 0.8fr;
        align-items: center;
        gap: 2rem;
    }
    .hero h1 {
        font-size: 2.5rem;
        line-height: 1.3;
        margin-bottom: 1rem;
        color: var(--c-text);
    }
    .hero h1 br { display: block; }
    .hero p {
        font-size: 1.1rem;
        color: var(--c-text-light);
    }
    .char-placeholder {
        width: 100%;
        height: 250px;
        background: rgba(255,255,255,0.5);
        border: 2px dashed #9ca3af;
        border-radius: 1rem;
        display: flex;
        justify-content: center;
        align-items: center;
        color: #666;
    }

    /* Section */
    .section-title {
        font-size: 1.5rem;
        margin-bottom: 1.5rem;
        padding-bottom: 0.5rem;
        border-bottom: 2px solid var(--c-primary);
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
    }
    .mt-5 { margin-top: 3rem; }

    /* Cards - Grid Layout */
    .article-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
        margin-bottom: 2rem;
    }
    
    .empty-state {
        text-align: center;
        padding: 2rem;
        background: #f9fafb;
        border-radius: 8px;
        color: var(--c-text-light);
        margin-bottom: 2rem;
    }

    /* List Card */
    .article-list {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
    .list-card {
        background: #fff;
        border: 1px solid var(--c-border);
        border-radius: 8px;
        transition: background 0.2s;
    }
    .list-card:hover {
        background: #f9fafb;
    }
    .list-link {
        display: flex;
        padding: 1rem;
        color: inherit;
        text-decoration: none !important;
        align-items: center;
        gap: 1rem;
    }
    .list-icon {
        font-size: 2rem;
        width: 60px;
        text-align: center;
    }
    .list-content h3 { margin: 0 0 0.2rem 0; font-size: 1.1rem; }
    .list-content p { margin: 0; font-size: 0.9rem; color: var(--c-text-light); }

    /* Buttons */
    .btn-outline {
        display: inline-block;
        padding: 0.8rem 2rem;
        border: 1px solid var(--c-border);
        border-radius: 9999px;
        color: var(--c-text);
        background: white;
        transition: all 0.2s;
        font-weight: bold;
    }
    .btn-outline:hover {
        border-color: var(--c-primary);
        color: var(--c-primary);
        text-decoration: none;
    }
    .more-link { text-align: center; }

    @media (max-width: 768px) {
        .main-container {
            grid-template-columns: 1fr;
        }
        .hero {
            grid-template-columns: 1fr;
            text-align: center;
        }
        .sidebar {
            order: 2;
        }
    }
</style>
